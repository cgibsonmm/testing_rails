<div class="new-link">
  <%= link_to 'Submit a new link', new_link_path %>
</div>

<div class="links-list">
  <ul>
    <% @links.each do |link| %>
      <div class="row">
        <li><%= link_to link.link_title, link.link_url %></li>
      </div>
    <% end %>
  </ul>
</div>
